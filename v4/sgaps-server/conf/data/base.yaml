# 데이터 기본 설정

# 데이터셋이 위치한 루트 디렉토리
# train.py 스크립트는 이 경로에서 .h5 파일을 검색합니다.
data_root: ${paths.data_root} # paths.data_root는 상위 config.yaml에 의해 './data'로 설정됩니다.

# 데이터 분할 비율 [학습용, 검증용]
# 예: [0.8, 0.2] -> 80%를 학습에, 20%를 검증에 사용
split_ratios: [0.8, 0.2]

# 모델이 처리할 기본 해상도
resolution: [112, 112]

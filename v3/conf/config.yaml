defaults:
    - model: base
    - data: base
    - training: gpu
    - evaluation: base
    - logging: base
    - output: default

hydra:
    run:
        dir: ${output.run_dir}
    sweep:
        dir: ${output.sweep_dir}
        subdir: ${hydra.job.num}
    output_subdir: null
